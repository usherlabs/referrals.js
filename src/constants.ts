export const TOKEN_EXPIRY_MINUTES = 43200 as const; // 30 days in minutes
export const TOKEN_NAME = "__usher_token" as const;
export const TOKEN_STORE_NAME = "__usher_store" as const;
export const USHER_REFERRED_NAME = "__usher_referred" as const;
